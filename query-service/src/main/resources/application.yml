server:
  port: 8080

spring:
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: 6379

management:
  endpoints:
    web:
      exposure:
        include: health, prometheus, info, metrics

logging:
  level:
    com.searchautocomplete: INFO
